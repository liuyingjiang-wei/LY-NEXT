---
title: ly-next project rules
---

## 默认输出结构

- 先结论：一句话说明将做什么/已做什么
- 再步骤：列出 2-5 条最关键的执行步骤
- 再验证：给出可以观察的检查点（命令/日志/文件变更）

## 项目内规则

- 变更保持聚焦：不要把不相关的重构混入同一次修改。
- 行为等价优先：重构以不改变外部行为为前提；若必须改变行为，要明确写出差异与验证方法。
- 示例命令必须可复制：文档里的命令不要写死本机绝对路径，优先相对路径与仓库根目录可运行。
- 可选依赖要可降级：Redis/DB/pgvector 不可用时尽量降级运行并给出明确日志。

## 质量门槛

- Python 代码必须通过：
  - `uv run ruff format .`
  - `uv run ruff check .`


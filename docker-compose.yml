# 编排已迁移至 docker/ 目录。
# 启动 Redis + PostgreSQL：docker compose -f docker/docker-compose.yml up -d
# 含应用镜像：docker compose -f docker/docker-compose.yml --profile app up -d --build
include:
  - path: docker/docker-compose.yml
